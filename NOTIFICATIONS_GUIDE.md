# تشغيل إشعارات التطبيق — الإصدار 26

رابط تطبيقك: [نظام الدعم الفني](https://anasmonti80-png.github.io/CTE-Jeddah-IT-Support/).

هذه تعليمات نشر خدمة الإرسال المطابقة للحزمة V26. لم تُنشر هذه النسخة إلى حسابك في هذه المراجعة؛ أمر الإعداد يحدّث Worker المسمى cte-jeddah-push ويربطه بالتطبيق الحالي. احتفظ بنسخة من الخادم السابق إن كنت تحتاج الرجوع إليه.

## 1. جهّز الحسابين مرة واحدة

1. أنشئ [حساب Cloudflare مجانيًا](https://dash.cloudflare.com/sign-up) وفعّل بريدك. استخدم **Workers Free**. لا تختَر خطة مدفوعة ولا تضف بطاقة لهذا الإعداد.
2. أبقِ Firebase على **Spark**. لن نستخدم Cloud Functions.
3. افتح [حسابات الخدمة في مشروعك](https://console.firebase.google.com/project/test-mode-c1433/settings/serviceaccounts/adminsdk). اختر **Firebase Admin SDK → Generate new private key**، ثم نزّل ملف JSON واحتفظ به خارج مجلد الموقع. سيقرأه أمر الإعداد من جهازك ويرفعه كـ Secret إلى خدمة Cloudflare الخاصة بحسابك. لا ترفعه إلى GitHub ولا ترسله في المحادثة.
4. افتح [Cloud Messaging في مشروعك](https://console.firebase.google.com/project/test-mode-c1433/settings/cloudmessaging). تأكد من تفعيل **Firebase Cloud Messaging API (V1)**. احتفظ بـ **Public VAPID Key** من **Web Push certificates**. إن كان موجودًا مسبقًا، استخدمه نفسه.
5. ثبّت [Node.js LTS](https://nodejs.org/) إن لم يكن موجودًا؛ يتطلب الأمر Node.js 22.13 أو أحدث.

## 2. شغّل أمر الإعداد

فك الضغط أولًا، ثم انقر مرتين على **START-PUSH.cmd**. أو افتح PowerShell داخل المجلد المفكوك وشغّل:

```powershell
.\START-PUSH.cmd
```

سيطلب مسار ملف JSON، ويفتح دخول Cloudflare. اختر حسابك المجاني ووافق على ربط أداة Wrangler الرسمية. سيقوم الأمر بإنشاء سجل منع التكرار، ورفع خدمة الإرسال، وحفظ المفتاح على الخادم، وتشغيل الفحص كل دقيقة، وتحديث `push-config.js` برابط خدمتك.

إذا كان لديك عدة حسابات Cloudflare وطلب تحديد أحدها، اختر الحساب المجاني الصحيح. يمكن إعادة الأمر بعد معالجة أي خطأ؛ يستخدم قاعدة `cte-push-state` الموجودة دون إنشاء نسخة أخرى. لا يحذف البلاغات أو يعيد ضبط سجل الإرسال.

## 3. حدّث التطبيق في GitHub Pages

افتح [المستودع](https://github.com/anasmonti80-png/CTE-Jeddah-IT-Support). ارفع **محتويات الحزمة** إلى المجلد الذي ينشر منه GitHub Pages، واستبدل الملفات المطابقة. ضع index.html مباشرة في جذر المستودع.

الملفات المطلوبة: `index.html`، `sw.js`، `push-client.js`، `push-config.js`، `manifest.json`، وأيقونات التطبيق والصورتان المرفقتان. مجلد cloudflare-push وأوامر الإعداد ملفات مصدر يمكن حفظها في المستودع؛ رفعها إلى GitHub لا ينشر الخادم. استثنِ node_modules و.wrangler وأي ملف مفتاح خاص عند الرفع بعد تشغيل الإعداد.

انتظر اكتمال نشر GitHub Pages، ثم افتح التطبيق وأعد تحميله. إن كانت نسخة قديمة مفتوحة، أغلقها وافتحها مرة أخرى. لا تحتاج حذف بيانات البلاغات أو حذف التطبيق.

## 4. التفعيل للمستخدمين

**المدير:** من زر تفعيل الإشعارات أعلى الصفحة، اضغط «إعداد Web Push» والصق المفتاح العام مرة واحدة. يُحفظ في Firebase لجميع الأجهزة. إذا كان محفوظًا مسبقًا، فلا حاجة لإدخاله مجددًا.

**أندرويد:** افتح الرابط في Chrome، وثبّت التطبيق من قائمة المتصفح إذا رغبت. سجّل الدخول ثم اضغط «تفعيل الإشعارات» واختر «سماح» مرة واحدة.

**آيفون وآيباد:** يتطلب iOS/iPadOS 16.4 أو أحدث. افتح الرابط في Safari، ثم مشاركة ← إضافة إلى الشاشة الرئيسية. افتح التطبيق من أيقونته، وسجّل الدخول، واضغط «تفعيل الإشعارات» ثم «سماح» مرة واحدة.

بعد ذلك يظهر **✓ الإشعارات مفعّلة**. يُجدّد التطبيق رمز الجهاز تلقائيًا عند الدخول، دون إعادة طلب الإذن. التفعيل مستقل لكل جهاز ومتصفح. قد يلزم السماح مجددًا إذا حذفت بيانات الموقع أو التطبيق أو سحبت الإذن من إعدادات النظام.

إغلاق التطبيق يبقي الإشعارات فعالة. **تسجيل الخروج** أو «إيقاف على هذا الجهاز» يوقف إشعارات الحساب على ذلك الجهاز. عند الدخول إلى حساب آخر يُربط الجهاز بالحساب الجديد.

## 5. اختبر الإرسال الحقيقي

1. تأكد من ظهور «الإشعارات مفعّلة» و«تعمل تلقائيًا كل دقيقة» معًا.
2. اضغط «اختبار على هذا الجهاز»، ثم اخرج إلى الشاشة الرئيسية دون تسجيل الخروج.
3. انتظر دقيقة أو دقيقتين. عند ضغط الإشعار يفتح رابط تطبيقك الصحيح.
4. جرّب بعد ذلك بلاغًا جديدًا عاديًا؛ يتلقى الإشعار المستخدم أو الأدوار المحددة أصلًا في نظام البلاغات.

الاختبار لا يرسل لجميع الموظفين. أول تشغيل للخدمة يتجاوز الإشعارات القديمة، لذا اختبر بعد ظهور جاهزية الخدمة. قد يستغرق انتشار الجدولة أول مرة حتى 15 دقيقة.

## إذا لم يصل الإشعار

| الظاهر | الإجراء |
|---|---|
| لم تُنشر خدمة الإرسال بعد | أكمل START-PUSH.cmd وارفع نسخة `push-config.js` التي حدّثها الأمر. |
| بانتظار أول تشغيل تلقائي | انتظر انتشار الجدولة. افتح رابط `/health` الذي طبعه الأمر. |
| `MISSING_SERVICE_ACCOUNT` | أعد أمر الإعداد بملف JSON الصحيح. |
| `GOOGLE_AUTH_400` | تحقق أن مفتاح حساب الخدمة لم يُلغَ وأن الملف سليم. |
| `DATABASE_READ_401` أو `DATABASE_READ_403` | حساب الخدمة يحتاج الوصول إلى Realtime Database في المشروع نفسه. لا تجعل قواعد قاعدة البيانات مفتوحة لحل المشكلة. |
| `FCM_403` | فعّل FCM HTTP V1، وتحقق من صلاحية Firebase Cloud Messaging API Admin لحساب الخدمة. |
| الإذن محظور | اسمح بالإشعارات من إعدادات التطبيق/المتصفح في الجهاز. لا يمكن للموقع تجاوز الرفض. |
| الجهاز مفعّل لكن لا يصل الاختبار | تحقق من الإنترنت، ووضع التركيز/عدم الإزعاج، وإعدادات إشعارات الجهاز، ثم أعد الاختبار بعد جاهزية `/health`. |

## حدود الخدمة المجانية

Firebase FCM مجاني، والخدمة معدّة لـ Cloudflare Workers Free وD1 Free. المجانية ضمن حصص هذه الخدمات، وليست استخدامًا غير محدود. أبقِ الحسابات على الخطط المجانية؛ لا يتضمن الأمر أي ترقية أو وسيلة دفع.

الفحص كل دقيقة، وليس إرسالًا فوريًا مضمونًا. تعالج الخدمة حتى 12 إرسالًا للأجهزة في كل دورة، وتحتفظ بالباقي للدورات التالية. تزيد المدة عند تراكم البلاغات أو كثرة الأجهزة. سجل الإرسال ومحاولات الإعادة معرّفان بمعرّف ثابت، ويمنع الجهاز تكرار الإشعارات المعتاد؛ لا يمكن ضمان وصول كل رسالة مرة واحدة بالضبط في كل ظروف الشبكة.

تقرأ الخدمة البلاغات الجديدة المكتوبة بمفاتيح Firebase `push()`، مع نافذة مراجعة للكتابات المتأخرة حتى خمس دقائق. استيراد سجلات تاريخية أو استعادة كتابات أقدم من ذلك ليس آلية لإرسال تنبيهات جديدة. لا تعدّل الخدمة حالات البلاغات أو تصميم التقارير.

## ما الذي تم اختباره؟

اختبارات محلية لواجهة التفعيل، ومنع التكرار، وتبديل الحساب، وتدوير الرموز، ورفض الإذن، وفشل الحفظ، واستعادة الإرسال، وصف الانتظار. جرى أيضًا تشغيل الخدمة وقاعدة D1 في محاكي Cloudflare الفعلي، مع اتصالات Firebase/FCM بديلة للاختبار. نجحت اختبارات التقارير وإعادة فتح البلاغات السابقة.

لم يُجرَ إرسال حي من Firebase أو اختبار على هاتف فعلي لهذه النسخة. تعذّر فحص `wrangler deploy --dry-run` داخل بيئة التنفيذ المقيدة بسبب وصول أداة البناء إلى مجلد أب؛ اختُبر تشغيل Worker مباشرة في Miniflare بدلًا منه.

## مراجع رسمية

- [أسعار Firebase وFCM](https://firebase.google.com/pricing)
- [حصص Workers](https://developers.cloudflare.com/workers/platform/limits/)
- [حصص D1](https://developers.cloudflare.com/d1/platform/pricing/)
- [جدولة Cloudflare](https://developers.cloudflare.com/workers/configuration/cron-triggers/)
- [مفاتيح Cloudflare السرية](https://developers.cloudflare.com/workers/configuration/secrets/)
- [مصادقة FCM من الخادم](https://firebase.google.com/docs/cloud-messaging/send/v1-api)
- [إشعارات تطبيقات الشاشة الرئيسية على iOS](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/)
